{
  "name": "webflow-scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=3000 --cors --no-browser",
    "dev:open": "live-server --port=3000 --cors"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
