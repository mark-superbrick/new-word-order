{
  "name": "webflow-scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --port 3000 --cors --no-open --files 'scripts/**/*.js'",
    "dev:open": "browser-sync start --server --port 3000 --cors --files 'scripts/**/*.js'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
